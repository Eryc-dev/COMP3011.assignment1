# Local development profile. Activate with: --spring.profiles.active=local
# (Eclipse: Run Configurations > Arguments, or env SPRING_PROFILES_ACTIVE=local).
# Set OPENAI_API_KEY in the run configuration's Environment tab - never in this file.

# Verbose logging of our own code while developing.
logging.level.comp3011.assignment1=DEBUG
# Show full startup detail locally.
spring.main.banner-mode=console
